services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: waldocs
      POSTGRES_PASSWORD: waldocs
      POSTGRES_DB: waldocs
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes: { pgdata: {} }
